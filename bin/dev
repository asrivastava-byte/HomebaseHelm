#!/usr/bin/env bash
set -euo pipefail
cd "$(dirname "$0")/.."

if ! command -v foreman >/dev/null; then
  echo "Installing foreman (one-time)..."
  gem install foreman
fi

exec foreman start -f Procfile.dev
